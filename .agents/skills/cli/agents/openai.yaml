interface:
  display_name: "Cli"
  short_description: "Repo-specific patterns and workflows for cli"
  default_prompt: "Use the cli repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true